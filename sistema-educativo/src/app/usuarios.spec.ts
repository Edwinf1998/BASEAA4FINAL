/* import { Estudiante } from './estudiante';

describe('Estudiante', () => {
  it('should create an instance', () => {
    expect(new Estudiante()).toBeTruthy();
  });
});
 */

import { Usuarios } from './usuarios';/*dice que es Usuarios*/ 

describe('Usuarios', () => {
  it('should create an instance', () => {
    expect(new Usuarios()).toBeTruthy();
  });
});


